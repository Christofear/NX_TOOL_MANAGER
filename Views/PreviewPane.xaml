<UserControl x:Class="NX_TOOL_MANAGER.Views.PreviewPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NX_TOOL_MANAGER.Views"
             Background="#2D2D2D">
    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <!-- ADDED: TextWrapping allows the label to go to a second line -->
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#4A4A4A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="250"/>
            <!-- Visual Area -->
            <RowDefinition Height="Auto"/>
            <!-- Separator -->
            <RowDefinition Height="*"/>
            <!-- Parameters Area -->
        </Grid.RowDefinitions>

        <local:ToolVisual Grid.Row="0" ToolData="{Binding SelectedRow, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        <Border Grid.Row="1" BorderBrush="#4A4A4A" BorderThickness="0,1,0,0"/>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Title -->
                <RowDefinition Height="*"/>
                <!-- List -->
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Parameters" FontWeight="SemiBold" FontSize="14" Margin="10,10,10,5"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Parameters}" Margin="10,0,10,10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <!-- UPDATED: Changed column widths to give more space to the TextBox -->
                                    <ColumnDefinition Width="2*"/>
                                    <!-- Label gets 2 parts of the space -->
                                    <ColumnDefinition Width="3*"/>
                                    <!-- TextBox gets 3 parts of the space -->
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" 
                                           Text="{Binding Definition.Description}" 
                                           Style="{StaticResource LabelStyle}"
                                           ToolTip="{Binding Definition.Description}"
                                           Margin="0,0,10,0"/>

                                <TextBox Grid.Column="1" 
                                         Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                         PreviewKeyDown="TextBox_PreviewKeyDown"
                                         PreviewTextInput="TextBox_PreviewTextInput"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>

