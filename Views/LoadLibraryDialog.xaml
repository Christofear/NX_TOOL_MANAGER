<Window x:Class="NX_TOOL_MANAGER.LoadLibraryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:NX_TOOL_MANAGER"
        xmlns:converters="clr-namespace:NX_TOOL_MANAGER.Converters"
        
        Height="Auto" Width="650"
        FontFamily="Segoe UI"
        Background="#1E1E1E"
        Foreground="White"

        WindowStyle="None" 
        AllowsTransparency="True"
        WindowStartupLocation="CenterOwner"
        SizeToContent="Height">

    <Window.Resources>
        <!-- (All existing styles are unchanged) -->
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="BrowseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#4A4A4A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Width" Value="230"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#454545"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2A2A2A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#4A4A4A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Width" Value="90"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#454545"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2A2A2A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ClearButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="Red"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#4A4A4A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <EventSetter Event="PreviewMouseDown" Handler="TextBox_PreviewMouseDown"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#4A4A4A" BorderThickness="1">
        <DockPanel>
            <Grid DockPanel.Dock="Top" Height="32" Background="#2D2D2D" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Load Library" Grid.Column="0" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#CCCCCC"/>
                <Button Grid.Column="1" Style="{StaticResource CloseButtonStyle}" Content="&#xE8BB;" Click="CloseButton_Click"/>
            </Grid>

            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20">
                <Button Content="Cancel" IsCancel="True" Style="{StaticResource ActionButtonStyle}" Margin="0,0,8,0"/>
                <Button Content="Load" IsDefault="True" Style="{StaticResource ActionButtonStyle}" Click="Load_Click"/>
            </StackPanel>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid Margin="20,20,20,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*" MinWidth="200"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.ColumnSpan="3" Text="Choose NX .dat files" FontWeight="SemiBold" Margin="0,0,0,16"/>

                    <Button Grid.Row="1" Grid.Column="0" Content="Tool_Database.dat" Style="{StaticResource BrowseButtonStyle}" Click="PickTools_Click" Margin="0,0,12,8"/>
                    <Grid Grid.Row="1" Grid.Column="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="ToolsTextBox" Grid.Column="0" Text="{Binding ToolsPath, UpdateSourceTrigger=PropertyChanged}" AllowDrop="True" PreviewDragOver="TextBox_PreviewDragOver" Drop="TextBox_Drop"/>
                        <Button Grid.Column="1" Content="✕" Style="{StaticResource ClearButtonStyle}" ToolTip="Clear Tools" Click="ClearTools_Click" Visibility="{Binding Text, ElementName=ToolsTextBox, Converter={StaticResource StringToVisibilityConverter}}"/>
                    </Grid>

                    <Button Grid.Row="2" Grid.Column="0" Content="Holder_Database.dat" Style="{StaticResource BrowseButtonStyle}" Click="PickHolders_Click" Margin="0,0,12,8"/>
                    <Grid Grid.Row="2" Grid.Column="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="HoldersTextBox" Grid.Column="0" Text="{Binding HoldersPath, UpdateSourceTrigger=PropertyChanged}" AllowDrop="True" PreviewDragOver="TextBox_PreviewDragOver" Drop="TextBox_Drop"/>
                        <Button Grid.Column="1" Content="✕" Style="{StaticResource ClearButtonStyle}" ToolTip="Clear Holders" Click="ClearHolders_Click" Visibility="{Binding Text, ElementName=HoldersTextBox, Converter={StaticResource StringToVisibilityConverter}}"/>
                    </Grid>

                    <Button Grid.Row="3" Grid.Column="0" Content="Shank_Database.dat" Style="{StaticResource BrowseButtonStyle}" Click="PickShanks_Click" Margin="0,0,12,8"/>
                    <Grid Grid.Row="3" Grid.Column="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="ShanksTextBox" Grid.Column="0" Text="{Binding ShanksPath, UpdateSourceTrigger=PropertyChanged}" AllowDrop="True" PreviewDragOver="TextBox_PreviewDragOver" Drop="TextBox_Drop"/>
                        <Button Grid.Column="1" Content="✕" Style="{StaticResource ClearButtonStyle}" ToolTip="Clear Shanks" Click="ClearShanks_Click" Visibility="{Binding Text, ElementName=ShanksTextBox, Converter={StaticResource StringToVisibilityConverter}}"/>
                    </Grid>

                    <!-- FIX: Added back the Trackpoint database row -->
                    <Button Grid.Row="4" Grid.Column="0" Content="Trackpoint_Database.dat" Style="{StaticResource BrowseButtonStyle}" Click="PickTrackpoints_Click" Margin="0,0,12,8"/>
                    <Grid Grid.Row="4" Grid.Column="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TrackpointsTextBox" Grid.Column="0" Text="{Binding TrackpointsPath, UpdateSourceTrigger=PropertyChanged}" AllowDrop="True" PreviewDragOver="TextBox_PreviewDragOver" Drop="TextBox_Drop"/>
                        <Button Grid.Column="1" Content="✕" Style="{StaticResource ClearButtonStyle}" ToolTip="Clear Trackpoints" Click="ClearTrackpoints_Click" Visibility="{Binding Text, ElementName=TrackpointsTextBox, Converter={StaticResource StringToVisibilityConverter}}"/>
                    </Grid>

                    <Button Grid.Row="5" Grid.Column="0" Content="Segmented_Tool_Database.dat" Style="{StaticResource BrowseButtonStyle}" Click="PickSegmentedTools_Click" Margin="0,0,12,8"/>
                    <Grid Grid.Row="5" Grid.Column="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="SegmentedToolsTextBox" Grid.Column="0" Text="{Binding SegmentedToolsPath, UpdateSourceTrigger=PropertyChanged}" AllowDrop="True" PreviewDragOver="TextBox_PreviewDragOver" Drop="TextBox_Drop"/>
                        <Button Grid.Column="1" Content="✕" Style="{StaticResource ClearButtonStyle}" ToolTip="Clear Segmented Tools" Click="ClearSegmentedTools_Click" Visibility="{Binding Text, ElementName=SegmentedToolsTextBox, Converter={StaticResource StringToVisibilityConverter}}"/>
                    </Grid>
                </Grid>
            </ScrollViewer>
        </DockPanel>
    </Border>
</Window>

